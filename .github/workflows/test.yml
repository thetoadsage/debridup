name: Verify

on:
  pull_request:
  push:
    branches:
      - main
  workflow_call:
    secrets:
      PRIVATE_PATTERNS:
        required: false

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before || '0000000000000000000000000000000000000000' }}
      CHANGE_TEXT: |
        ${{ github.event.pull_request.title }}
        ${{ github.event.pull_request.body }}
      PRIVATE_PATTERNS: ${{ secrets.PRIVATE_PATTERNS }}
    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version: "1.25.14"

      - name: Set up Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: "22"

      - name: Verify Go formatting
        shell: sh
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            printf '%s\n' "$unformatted"
            exit 1
          fi

      - name: Run Go tests
        run: go test ./...

      - name: Run race-enabled Go tests
        run: go test -race ./...

      - name: Run Go static analysis
        run: go vet ./...

      - name: Run browser module tests
        run: node --test cmd/debridup/web/dashboard-model.test.mjs

      - name: Install vulnerability scanner
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.7.0

      - name: Scan Go dependencies
        run: govulncheck ./...

      - name: Build container image
        run: docker build -t debridup:test .

      - name: Install image metadata scanner
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends libimage-exiftool-perl

      - name: Check release safety
        run: sh scripts/check-release-safety.sh "$BASE_SHA"
